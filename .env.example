# --- web app ---
DATABASE_URL=postgres://dashboards:dashboards@localhost:5432/dashboards
AUTH_SECRET=change-me            # openssl rand -base64 32
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_URL=http://localhost:3000
SITE_URL=http://localhost:3000

# --- one-time Grafana migration (scripts/migrate-grafana) ---
DD_PAT=                          # Datadog personal access token (Bearer). Never commit.
DD_SITE=datadoghq.com
DOGSTATSD_HOST=                  # host running a Datadog Agent with DogStatsD (non-local traffic enabled)
DOGSTATSD_PORT=8125
