# --- web app ---
DATABASE_URL=postgres://dashboards:dashboards@localhost:5432/dashboards
AUTH_SECRET=change-me            # openssl rand -base64 32
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
AUTH_URL=http://localhost:3000
SITE_URL=http://localhost:3000

# Comments and reactions via GitHub Discussions (giscus). Public identifiers, safe to keep.
GISCUS_REPO=Datadog-Korea-User-Group/datadog-dashboards
GISCUS_REPO_ID=R_kgDOULXkRA
GISCUS_CATEGORY=Announcements
GISCUS_CATEGORY_ID=DIC_kwDOULXkRM4DEsH0

# --- one-time Grafana migration (scripts/migrate-grafana) ---
DD_PAT=                          # Datadog personal access token (Bearer), or set DD_API_KEY + DD_APP_KEY. Never commit.
DD_API_KEY=
DD_APP_KEY=
DD_SITE=datadoghq.com
DOGSTATSD_HOST=                  # host running a Datadog Agent with DogStatsD (non-local traffic enabled)
DOGSTATSD_PORT=8125
